Run `nixos-rebuild build --flake .#nesco 2>&1`, `nixos-rebuild build --flake .#fresco 2>&1`, and `nixos-rebuild build --flake .#medesco 2>&1` (in parallel). Collect any warnings from the output (lines containing "warning:" or "trace:"). For each warning, identify the source file and fix the underlying issue. After applying fixes, rebuild all three hosts again to verify the warnings are resolved.
